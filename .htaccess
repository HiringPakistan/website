# Forces HTTPS

RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301] 

# 404 redirect

ErrorDocument 404 http://hiringpakistan.netlify.app/index.html

# 500 redirect

ErrorDocument 500 http://hiringpakistan.netlify.app/index.html


# Disables directory indexing (being able to view a list of files instead of the actual webpage)

Options -Indexes